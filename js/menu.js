$(function(){

      var header = document.getElementById('header');
      var headroom = new Headroom(header);
       headroom.init();
       //menu responsive
       var ancho = $(window).width(),
       enlaces = $('#enlaces'),
       btnMenu = $('btnMenu'),
       icono = $('btnMenu .icono');



});
